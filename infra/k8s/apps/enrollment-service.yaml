apiVersion: v1
kind: Service
metadata:
  name: enrollment-service
  namespace: mse-mooc
  labels:
    app.kubernetes.io/name: enrollment-service
spec:
  selector:
    app.kubernetes.io/name: enrollment-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: enrollment-service
  namespace: mse-mooc
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: enrollment-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: enrollment-service
        app.kubernetes.io/component: backend
    spec:
      initContainers:
        - name: wait-for-postgres-kafka-redis
          image: busybox:1.36
          command:
            - sh
            - -ec
            - |
              until nc -z postgres 5432; do echo waiting for postgres; sleep 2; done
              until nc -z kafka 9092; do echo waiting for kafka; sleep 2; done
              until nc -z redis 6379; do echo waiting for redis; sleep 2; done
      containers:
        - name: enrollment-service
          image: mse-mooc/enrollment-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
              name: http
          env:
            - name: ENROLLMENT_SERVICE_PORT
              valueFrom:
                configMapKeyRef:
                  name: mse-mooc-app-config
                  key: ENROLLMENT_SERVICE_PORT
          envFrom:
            - configMapRef:
                name: mse-mooc-app-config
            - secretRef:
                name: mse-mooc-secrets
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
